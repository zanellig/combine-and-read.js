{
  "scripts": {
    "ocr": "node src/index.js",
    "csv": "node src/csv.js",
    "start": "pip install -r requirements.txt && npm install && npm run ocr && npm run csv"
  },
  "dependencies": {
    "tesseract.js": "^5.1.0"
  }
}
